pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Jitpack kütüphane motoru için şart
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "UrlDownloader"
include ':app'
